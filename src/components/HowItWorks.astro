<section id="how-it-works">
	<div class="container">
		<div class="section-header">
			<span class="badge">Cómo funciona</span>
			<h2>Ponlo a trabajar en minutos</h2>
			<p class="section-subtitle">
				Tres pasos simples para integrar SalesPro Agent en tu organización
				y empezar a automatizar tu operación.
			</p>
		</div>

		<div class="steps">
			<div class="step">
				<div class="step-number">1</div>
				<div class="step-content">
					<h3>Conecta tus herramientas</h3>
					<p>Integra tu CRM, calendario y plataformas de comunicación con un solo clic. Sin configuraciones complejas.</p>
				</div>
			</div>
			<div class="step-connector">
				<div class="connector-line"></div>
			</div>
			<div class="step">
				<div class="step-number">2</div>
				<div class="step-content">
					<h3>Configura tus reglas</h3>
					<p>Define flujos de trabajo, prioridades y objetivos. El agente aprende tu estilo y se adapta a tus procesos.</p>
				</div>
			</div>
			<div class="step-connector">
				<div class="connector-line"></div>
			</div>
			<div class="step">
				<div class="step-number">3</div>
				<div class="step-content">
					<h3>Automatiza y escala</h3>
					<p>El agente trabaja 24/7: prospecta, da seguimiento, genera reportes y mantiene a tu equipo sincronizado.</p>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	#how-it-works {
		padding: 100px 0;
		background: var(--bg-secondary);
		transition: background 0.3s ease;
	}

	.section-header {
		text-align: center;
		margin-bottom: 64px;
	}

	.badge {
		display: inline-block;
		padding: 6px 16px;
		border-radius: 100px;
		background: var(--badge-bg);
		color: var(--badge-text);
		font-size: 14px;
		font-weight: 600;
		letter-spacing: 0.02em;
		margin-bottom: 20px;
		transition: background 0.3s ease, color 0.3s ease;
	}

	h2 {
		font-size: 40px;
		font-weight: 800;
		color: var(--text-primary);
		margin-bottom: 16px;
		letter-spacing: -0.02em;
		transition: color 0.3s ease;
	}

	.section-subtitle {
		font-size: 18px;
		color: var(--text-secondary);
		max-width: 600px;
		margin: 0 auto;
		transition: color 0.3s ease;
	}

	.steps {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0;
		max-width: 640px;
		margin: 0 auto;
	}

	.step {
		display: flex;
		align-items: center;
		gap: 28px;
		width: 100%;
	}

	.step-number {
		flex-shrink: 0;
		width: 56px;
		height: 56px;
		border-radius: 50%;
		background: var(--step-number-bg);
		color: var(--step-number-text);
		font-size: 22px;
		font-weight: 800;
		display: flex;
		align-items: center;
		justify-content: center;
		transition: background 0.3s ease, color 0.3s ease;
	}

	.step-content h3 {
		font-size: 22px;
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: 8px;
		transition: color 0.3s ease;
	}

	.step-content p {
		font-size: 16px;
		color: var(--text-secondary);
		line-height: 1.7;
		transition: color 0.3s ease;
	}

	.step-connector {
		display: flex;
		justify-content: center;
		padding: 8px 0;
		width: 56px;
	}

	.connector-line {
		width: 2px;
		height: 32px;
		background: var(--connector-bg);
		border-radius: 1px;
		transition: background 0.3s ease;
	}

	@media (max-width: 600px) {
		h2 {
			font-size: 28px;
		}

		#how-it-works {
			padding: 64px 0;
		}

		.step {
			gap: 20px;
		}

		.step-number {
			width: 44px;
			height: 44px;
			font-size: 18px;
		}

		.step-content h3 {
			font-size: 18px;
		}

		.step-connector {
			width: 44px;
		}
	}
</style>
