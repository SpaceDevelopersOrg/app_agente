<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content="Agente de IA especializado en ventas y automatización de tareas organizacionales." />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
		<meta name="generator" content={Astro.generator} />
		<title>SalesPro Agent — IA para Ventas y Organización</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	:root {
		--black: #000000;
		--ebony: #5b6057;
		--dusty-olive: #6e9075;
		--mint-leaf: #78c091;
		--neon-ice: #81f0e5;
	}

	[data-theme="light"],
	:root {
		--bg: #ffffff;
		--bg-secondary: #f8fbf9;
		--bg-card: #ffffff;
		--text-primary: #000000;
		--text-secondary: #5b6057;
		--border: #e8f0ec;
		--badge-bg: #6e9075;
		--badge-text: #ffffff;
		--btn-primary-bg: #000000;
		--btn-primary-text: #ffffff;
		--btn-secondary-border: #000000;
		--btn-secondary-text: #000000;
		--hero-bg: linear-gradient(160deg, #f0faf4 0%, #e8f5ef 40%, #dceee3 100%);
		--section-alt-bg: #f8fbf9;
		--card-shadow: rgba(0, 0, 0, 0.06);
		--hover-shadow: rgba(0, 0, 0, 0.08);
		--step-number-bg: #000000;
		--step-number-text: #ffffff;
		--connector-bg: #000000;
		--stats-bg: #000000;
		--footer-bg: #000000;
	}

	[data-theme="dark"] {
		--bg: #0f0f0f;
		--bg-secondary: #161616;
		--bg-card: #1c1c1c;
		--text-primary: #f0f0f0;
		--text-secondary: #9ca3af;
		--border: #2a2a2a;
		--badge-bg: #81f0e5;
		--badge-text: #000000;
		--btn-primary-bg: #ffffff;
		--btn-primary-text: #000000;
		--btn-secondary-border: #ffffff;
		--btn-secondary-text: #ffffff;
		--hero-bg: linear-gradient(160deg, #0f1f14 0%, #1a2a1f 40%, #0f1f14 100%);
		--section-alt-bg: #161616;
		--card-shadow: rgba(0, 0, 0, 0.3);
		--hover-shadow: rgba(0, 0, 0, 0.4);
		--step-number-bg: #81f0e5;
		--step-number-text: #000000;
		--connector-bg: #81f0e5;
		--stats-bg: #1c1c1c;
		--footer-bg: #1c1c1c;
	}

	@media (prefers-color-scheme: dark) {
		:root {
			--bg: #0f0f0f;
			--bg-secondary: #161616;
			--bg-card: #1c1c1c;
			--text-primary: #f0f0f0;
			--text-secondary: #9ca3af;
			--border: #2a2a2a;
			--badge-bg: #81f0e5;
			--badge-text: #000000;
			--btn-primary-bg: #ffffff;
			--btn-primary-text: #000000;
			--btn-secondary-border: #ffffff;
			--btn-secondary-text: #ffffff;
			--hero-bg: linear-gradient(160deg, #0f1f14 0%, #1a2a1f 40%, #0f1f14 100%);
			--section-alt-bg: #161616;
			--card-shadow: rgba(0, 0, 0, 0.3);
			--hover-shadow: rgba(0, 0, 0, 0.4);
			--step-number-bg: #81f0e5;
			--step-number-text: #000000;
			--connector-bg: #81f0e5;
			--stats-bg: #1c1c1c;
			--footer-bg: #1c1c1c;
		}
	}

	html {
		scroll-behavior: smooth;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
		color: var(--text-primary);
		background: var(--bg);
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
		transition: background 0.3s ease, color 0.3s ease;
	}

	*,
	*::before,
	*::after {
		box-sizing: border-box;
	}

	h1, h2, h3, h4, h5, h6 {
		margin: 0;
		font-weight: 700;
		line-height: 1.2;
	}

	p {
		margin: 0;
		line-height: 1.6;
	}

	a {
		color: inherit;
		text-decoration: none;
	}

	.container {
		max-width: 1100px;
		margin: 0 auto;
		padding: 0 24px;
	}

	@media (max-width: 768px) {
		.container {
			padding: 0 16px;
		}
	}
</style>
